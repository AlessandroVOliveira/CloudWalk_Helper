# CloudWalk Helper Environment Variables

# LLM Provider: 'openrouter' (recommended) or 'ollama' (local)
LLM_PROVIDER=openrouter

# OpenRouter API Key (get free key at https://openrouter.ai)
OPENROUTER_API_KEY=your_api_key_here

# Optional: Ollama settings (only if LLM_PROVIDER=ollama)
# OLLAMA_MODEL=llama3.2:3b
